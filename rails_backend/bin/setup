#!/bin/bash

echo "Setting up Rails backend..."

# Install gems
bundle install

# Create database directories
mkdir -p db
mkdir -p public/uploads
mkdir -p backups

# Run database migrations
rails db:create db:migrate

echo "Setup complete!"
echo "Run 'rails server -p 3001' to start the server"
