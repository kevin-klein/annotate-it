#!/bin/bash

cd "$(dirname "$0")/.."

echo "Starting Rails backend..."

# Ensure database exists and is migrated
if [ ! -f "db/development.sqlite3" ]; then
  echo "Creating database..."
  rails db:create db:migrate
fi

# Start server
exec rails server -b 0.0.0.0 -p 3001
